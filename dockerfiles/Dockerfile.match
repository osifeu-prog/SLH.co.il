FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir aiogram==3.26.0 asyncpg python-dotenv
COPY shared/ /app/shared/
COPY match-bot/ /app/match-bot/
ENV PYTHONPATH="/app:/app/shared"
CMD ["python", "/app/match-bot/main.py"]
