# SLH SPARK — Closeout Report
**Date:** 2026-04-22

## What was completed

### 1. Ledger database fault was resolved
The `slh-ledger` service had been failing with `UndefinedTableError: relation "event_log" does not exist`. The issue was traced through DB name, schema visibility, and runtime packaging until the real fault path was isolated. `event_log` was confirmed to exist in `slh_main`, and after the DB alignment work the ledger stopped producing `UndefinedTableError` and `ledger_listener loop error` in recent logs. fileciteturn8file0 fileciteturn11file0

### 2. `shared_db_core.py` packaging defect was fixed for ledger
`shared_db_core.py` existed locally in `D:\SLH_ECOSYSTEM` but was missing inside the `slh-ledger` container, while the image only copied `shared/`. `dockerfiles/Dockerfile.template` was updated so that `shared_db_core.py` is copied into `/app/`. The ledger image was rebuilt and redeployed. After that, the module became importable inside the container and the old `shared_db_core unavailable` warning disappeared from the recent log output. fileciteturn11file0 fileciteturn13file0

### 3. Control layer now works in-session through `slh-control.ps1`
The canonical control path was restored in the active session by rebinding `slh-start` and `slh-logs` to `D:\SLH_ECOSYSTEM\ops\slh-control.ps1`. The following worked successfully:
- `slh-logs slh-ledger`
- `slh-doctor`
- `slh-health`

This verified that the control tree itself is functional. fileciteturn19file0turn19file1

### 4. Persistent workaround created for broken PowerShell profile loading
A stable profile shim was written to:
- `D:\SLH_ECOSYSTEM\ops\slh-profile-clean.ps1`

Dot-sourcing this file in a new PowerShell session correctly restores the clean command layer:
```powershell
. D:\SLH_ECOSYSTEM\ops\slh-profile-clean.ps1
```
This is currently the reliable startup path until the OneDrive-backed PowerShell profile behavior is cleaned up fully. fileciteturn19file0turn19file1

## Current validated state
- Docker runtime: healthy overall
- `slh-postgres`: healthy
- `slh-redis`: healthy
- `slh-ledger`: running and no recent `UndefinedTableError`
- `shared_db_core`: importable in ledger container
- `event_log`: present in `slh_main`
- `slh-control.ps1`: functional in-session
- `slh-profile-clean.ps1`: functional when dot-sourced

## Open items

### A. PowerShell profile persistence is still dirty
The official PowerShell profile path still contains the old definitions mapping:
- `slh-start -> start.ps1`
- `slh-logs -> logs.ps1`

This means a fresh PowerShell window may still load the legacy aliases until the host/user profile chain is cleaned permanently. The safe operational workaround is to dot-source:
```powershell
. D:\SLH_ECOSYSTEM\ops\slh-profile-clean.ps1
```
fileciteturn18file0turn19file1

### B. Local API health endpoint on `8080` is not valid at `/api/health`
`slh-health` shows that `http://127.0.0.1:8080/api/health` returns `404`. This is not blocking the ledger repair, but it means the local API health contract still needs to be normalized. fileciteturn19file1

### C. Guardian should be rechecked after the shared core fix pattern
Recent guardian error scan was clean in the control health output, but its long-term health contract remains weaker than desired. It should be included in the next pass of runtime hardening. fileciteturn19file1

## New operational files created
- `D:\SLH_ECOSYSTEM\ops\slh-control.ps1`
- `D:\SLH_ECOSYSTEM\ops\slh-profile-clean.ps1`
- `slh-release.ps1` (template attached in this closeout package)

## Recommended startup flow for the next session
```powershell
. D:\SLH_ECOSYSTEM\ops\slh-profile-clean.ps1
slh-doctor
slh-health
slh-status
```

## Recommended next engineering priorities
1. Fix persistent PowerShell profile loading so the clean control tree works without dot-sourcing.
2. Normalize local API health routing on port `8080`.
3. Add `slh-release` to the operational toolchain.
4. Add stronger health contracts for guardian and selected bot services.
5. Start the UI/UX + CI/CD hardening pass from a now-stable control baseline.
