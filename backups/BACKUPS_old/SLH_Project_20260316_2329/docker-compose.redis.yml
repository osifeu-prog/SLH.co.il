services:
  redis:
    image: redis:7-alpine
    container_name: slh_redis
    ports:
      - "6380:6379"
    command: ["redis-server","--appendonly","yes"]
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
